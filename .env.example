# Variáveis de Ambiente para o Supabase
# Copie este arquivo para .env.local e preencha com suas chaves do projeto Supabase.
NEXT_PUBLIC_SUPABASE_URL="SUA_SUPABASE_URL_AQUI"
NEXT_PUBLIC_SUPABASE_ANON_KEY="SUA_SUPABASE_ANON_KEY_AQUI"

# Ferramentas de Desenvolvimento
# Defina como 'true' para habilitar páginas e funcionalidades de desenvolvimento,
# como a página de seeding de dados em /dev/seed.
# Remova ou defina como 'false' em produção.
NEXT_PUBLIC_DEV_TOOLS="true"

# -----------------------------------------------------------------------------
# Configurações do Servidor (NUNCA expor no lado do cliente)
# -----------------------------------------------------------------------------
# Chave de serviço do Supabase. Permite bypassar as políticas de RLS.
# ⚠️ CRÍTICO: NUNCA use prefixo NEXT_PUBLIC_ nesta variável!
# Use apenas em rotas de API e Server Actions.
# Encontrada em: Supabase Dashboard → Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVycWxwem9kZGZlb3hidm5mcWdtIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2MTE4MjAxMSwiZXhwIjoyMDc2NzU4MDExfQ.TdvcioD9OfE8Tir1WktN3BEaaPxmIXi8dNapyas7VqY

# -----------------------------------------------------------------------------
# Configurações de Admin
# -----------------------------------------------------------------------------
# Lista de emails autorizados a criar novos usuários (separados por vírgula).
# Apenas esses emails poderão acessar a rota /api/admin/create-user.
# Exemplo: admin@empresa.com,outro-admin@empresa.com
NEXT_PUBLIC_ADMIN_WHITELIST=seu-email@dominio.com

# -----------------------------------------------------------------------------
# Configurações de Webhooks
# -----------------------------------------------------------------------------
# ID do usuário padrão para webhooks (MVP mono-usuário).
# Encontrado em Supabase -> Authentication -> Users -> User UID.
WEBHOOK_DEFAULT_USER_ID=SEU_AUTH_USER_ID

# Segredo para validar a assinatura do webhook da Kiwify.
KIWIFY_WEBHOOK_SECRET=SEU_SEGREDO_DA_KIWIFY
